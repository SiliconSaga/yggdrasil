# plain — Claude Code

Personal vault organized via the para method (lowercase).
The detection regex is case-insensitive on BOTH alternatives —
`claudesidian` AND `PARA Method`. This fixture pins the lowercase
form of the PARA alternative so a regression that drops the `-i`
flag from grep would fail this test loudly.
