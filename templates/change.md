> **AI-assisted change proposal.** Filed by agent driven by @HUMAN_ACCOUNT via [GDD](@GDD_HOME).

## Summary

- [ONE LINE PER PARAGRAPH AND PER BULLET — no hard wrapping; renderers wrap. What this does and why — one bullet per logical change. Pitch it one level above the commits and never re-narrate them: the provider renders commit messages right below this body, so duplicated prose makes reviewers read everything twice. Budget: absorbable in ~30 seconds; style.changeNotes in ecosystem config (terse|standard|detailed, shown by `ws orient`) tunes it. Register — how this reads to whoever receives it — is set by comms.flavor and shown by `ws orient`; see docs/gdd/agent-communication.md.]

## Test plan

- [ ] [How to verify the change works]

## Related

- Closes #N (if applicable, otherwise remove this line)
