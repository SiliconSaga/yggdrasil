# Supplemental Notes

<!-- This file has no sadd_section: frontmatter key, so it is NOT picked up
     by ws project status/build. Use files like this for research notes,
     links, or scratch content that lives alongside a section but should not
     appear in the assembled document. -->
